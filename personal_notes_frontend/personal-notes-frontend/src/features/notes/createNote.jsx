const CreateNote = () => {
  return (
    <div className="page-container">
      <h2>Create Note</h2>
      <p>The create note screen is ready to build.</p>
    </div>
  );
};

export default CreateNote;
